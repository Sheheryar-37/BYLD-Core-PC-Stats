<UserControl x:Class="PcStatsMonitor.Controls.WeatherScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="854" d:DesignWidth="480">

    <UserControl.Resources>
        <SolidColorBrush x:Name="ForecastCardBackground" x:Key="ForecastCardBackground" Color="#14888888"/>
        <SolidColorBrush x:Name="ForecastTabActiveForeground" x:Key="ForecastTabActiveForeground" Color="White"/>
    </UserControl.Resources>

    <Grid x:Name="RootWeatherGrid" Width="480" Height="854">
        <!-- Main Background and Theme Support -->
        <Border x:Name="WeatherBg" Background="#121212">
            <Grid Margin="24,140,24,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Header -->
                    <RowDefinition Height="Auto"/> <!-- Hero Card -->
                    <RowDefinition Height="Auto"/> <!-- Stats Row -->
                    <RowDefinition Height="Auto"/> <!-- Forecast Tabs -->
                    <RowDefinition Height="Auto"/> <!-- Hourly Forecast -->
                    <RowDefinition Height="*"/>    <!-- Graph Area -->
                </Grid.RowDefinitions>

                <!-- 1. Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,24">
                    <TextBlock x:Name="TxtDateHeader" Text="14 August 2024" Foreground="#888888" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBlock x:Name="TxtCity" Text="LOADING..." Foreground="White" FontSize="24" FontWeight="Bold"/>
                </StackPanel>

                <!-- 2. Hero Card -->
                <Border x:Name="HeroCard" Grid.Row="1" Background="#1E1E1E" CornerRadius="24" Padding="24" Margin="0,0,0,20">
                    <Grid>
                        <StackPanel>
                            <TextBlock x:Name="TxtCondition" Text="Cloudy" Foreground="#888888" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="TxtTemp" Text="18°C" Foreground="White" FontSize="48" FontWeight="Bold"/>
                            </StackPanel>
                        </StackPanel>
                        <!-- Large Icon -->
                        <TextBlock x:Name="TxtWeatherIconLarge" Text="&#xE709;" FontFamily="Segoe MDL2 Assets" Foreground="White" FontSize="80" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,0,0"/>
                    </Grid>
                </Border>

                <!-- 3. Bottom Section Background Box -->
                <Border Grid.Row="2" Grid.RowSpan="4" Background="#14888888" CornerRadius="28" Padding="16,20,16,16" Margin="0,10,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/> <!-- Stats Row -->
                            <RowDefinition Height="Auto"/> <!-- Forecast Tabs -->
                            <RowDefinition Height="Auto"/> <!-- Forecast List -->
                            <RowDefinition Height="*"/>    <!-- Graph -->
                        </Grid.RowDefinitions>

                        <!-- Stats Row (Wind, Humidity, Rain) -->
                        <Grid Grid.Row="0" Margin="0,0,0,32">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                <TextBlock x:Name="TxtWind" Text="10 km/h" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Wind" Foreground="#888888" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                <TextBlock x:Name="TxtHumidity" Text="98%" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Humidity" Foreground="#888888" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                <TextBlock x:Name="TxtPressure" Text="100%" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="Rain" Foreground="#888888" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>

                        <!-- Forecast Tabs -->
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
                            <StackPanel.Resources>
                                <Style TargetType="RadioButton">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="#888888"/>
                                    <Setter Property="FontSize" Value="16"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Margin" Value="0,0,24,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="RadioButton">
                                                <StackPanel Background="Transparent">
                                                    <ContentPresenter Content="{TemplateBinding Content}"/>
                                                    <Ellipse x:Name="Indicator" Width="4" Height="4" Fill="{TemplateBinding Foreground}" HorizontalAlignment="Center" Margin="0,4,0,0" Visibility="Hidden"/>
                                                </StackPanel>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter Property="Foreground" Value="{DynamicResource ForecastTabActiveForeground}"/>
                                                        <Setter TargetName="Indicator" Property="Visibility" Value="Visible"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </StackPanel.Resources>
                            <RadioButton x:Name="TabToday" Content="Today" IsChecked="True" Checked="ForecastTab_Checked"/>
                            <RadioButton x:Name="TabTomorrow" Content="Tomorrow" Checked="ForecastTab_Checked"/>
                            <RadioButton x:Name="TabNextDays" Content="Next 5 Days" Checked="ForecastTab_Checked"/>
                        </StackPanel>

                        <!-- Forecast List (Scrollable) -->
                        <ScrollViewer x:Name="ForecastScroller" Grid.Row="2" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" PreviewMouseWheel="ForecastScroller_PreviewMouseWheel">
                            <ItemsControl x:Name="LstForecast">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border x:Name="ForecastCard" Background="{DynamicResource ForecastCardBackground}" CornerRadius="16" Padding="12" Margin="0,0,12,0" Width="90">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding Day}" Foreground="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Foreground}" Opacity="0.6" FontSize="11" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                                <TextBlock Text="{Binding Icon}" FontFamily="Segoe MDL2 Assets" Foreground="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Foreground}" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                                <TextBlock Text="{Binding Temp}" Foreground="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Foreground}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <!-- Bottom Graph -->
                        <Grid Grid.Row="3" Margin="0,30,0,0">
                            <Path x:Name="GraphPath" Stroke="White" StrokeThickness="2" Opacity="0.3" Stretch="Fill" Height="80" VerticalAlignment="Bottom" Data="M 0,100 C 50,80 100,90 150,50 S 250,20 300,60 S 400,90 480,40"/>
                            <Ellipse x:Name="GraphPoint" Fill="White" Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,40,30">
                                <Ellipse.Effect>
                                    <DropShadowEffect BlurRadius="10" Color="White" ShadowDepth="0"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <Border x:Name="GraphTooltip" Background="#1E1E1E" CornerRadius="8" Padding="8,4" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,20,50" BorderBrush="#33FFFFFF" BorderThickness="1">
                                <TextBlock Text="19.5 km" Foreground="White" FontSize="10" FontWeight="Bold"/>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Loading Indicator -->
        <Border x:Name="LoadingOverlay" VerticalAlignment="Top" Height="3" Background="#33FFFFFF" Visibility="Collapsed">
            <ProgressBar IsIndeterminate="True" Height="3" BorderThickness="0" Background="Transparent" Foreground="#3b82f6"/>
        </Border>
    </Grid>
</UserControl>
