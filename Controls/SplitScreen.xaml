<UserControl x:Class="PcStatsMonitor.Controls.SplitScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:PcStatsMonitor.Controls"
             Width="480" Height="854"
             Background="Transparent"
             FontFamily="{Binding Theme.FontFamily, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue='Segoe UI'}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <!-- Compact glass tile used by the stats half -->
        <Style x:Key="StatTile" TargetType="Border">
            <Setter Property="Background" Value="#0CFFFFFF"/>
            <Setter Property="BorderBrush" Value="#1AFFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
        </Style>
        <Style x:Key="TileLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{Binding Theme.ForegroundColor, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue='#FFFFFF'}"/>
            <Setter Property="Opacity" Value="0.45"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="TileValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{Binding Theme.ForegroundColor, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue='#FFFFFF'}"/>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="TileUnit" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{Binding Theme.ForegroundColor, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue='#FFFFFF'}"/>
            <Setter Property="Opacity" Value="0.45"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="Margin" Value="3,0,0,4"/>
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{Binding Theme.ForegroundColor, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue='#FFFFFF'}"/>
            <Setter Property="FontSize" Value="19"/>
            <Setter Property="FontWeight" Value="Heavy"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <!-- Top margin clears the BYLD logo, matching the other 7" screens. -->
    <Grid Margin="26,170,26,70">
        <Grid.RowDefinitions>
            <!-- Upper half: system stats. Lower half: cooling. -->
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── Stats half ───────────────────────────────────────────────── -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel HorizontalAlignment="Center" Margin="0,0,0,16">
                <TextBlock Text="SYSTEM STATS" Style="{StaticResource SectionTitle}"/>
                <Border Height="3" Width="46" CornerRadius="2" Margin="0,7,0,0"
                        Background="{Binding Theme.AccentColorBrush, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>

            <!-- Real gauges, same control the Gauges dashboard uses, so this half carries the
                 graphs and colour the plain text tiles were missing (client round 19, item 5). -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <controls:DualCircularGauge Grid.Row="0" Grid.Column="0"
                    Width="180" Height="180" Margin="0,0,4,6"
                    Visibility="{Binding Theme.IsCpuEnabled, Converter={StaticResource BoolToVis}}"
                    CenterLabelString="CPU TEMP"
                    CenterTextString="{Binding Metrics.CpuTemp, StringFormat={}{0:0}°C, FallbackValue=0°C}"
                    LeftValue="{Binding Metrics.CpuTemp, FallbackValue=0}"
                    RightLabelTextString="CPU LOAD"
                    RightValueTextString="{Binding Metrics.CpuLoad, StringFormat={}{0:0}%, FallbackValue=0%}"
                    RightValue="{Binding Metrics.CpuLoad, FallbackValue=0}"/>

                <controls:DualCircularGauge Grid.Row="0" Grid.Column="1"
                    Width="180" Height="180" Margin="4,0,0,6"
                    Visibility="{Binding Theme.IsGpuEnabled, Converter={StaticResource BoolToVis}}"
                    CenterLabelString="GPU TEMP"
                    CenterTextString="{Binding Metrics.GpuTemp, StringFormat={}{0:0}°C, FallbackValue=0°C}"
                    LeftValue="{Binding Metrics.GpuTemp, FallbackValue=0}"
                    RightLabelTextString="GPU LOAD"
                    RightValueTextString="{Binding Metrics.GpuLoad, StringFormat={}{0:0}%, FallbackValue=0%}"
                    RightValue="{Binding Metrics.GpuLoad, FallbackValue=0}"/>

                <!-- Memory bar spanning both columns, tinted with the accent colour -->
                <Border Grid.Row="1" Grid.ColumnSpan="2" Style="{StaticResource StatTile}" Margin="0,2,0,0">
                    <StackPanel>
                        <Grid>
                            <TextBlock Text="MEMORY" Style="{StaticResource TileLabel}"/>
                            <TextBlock HorizontalAlignment="Right" Style="{StaticResource TileLabel}"
                                       Text="{Binding Metrics.RamLoad, StringFormat={}{0:0}%, FallbackValue=0%}"/>
                        </Grid>
                        <ProgressBar Height="8" Margin="0,7,0,0" Minimum="0" Maximum="100"
                                     Value="{Binding Metrics.RamLoad, Mode=OneWay, FallbackValue=0}"
                                     Foreground="{Binding Theme.AccentColorBrush, RelativeSource={RelativeSource AncestorType=Window}}"
                                     Background="#22FFFFFF" BorderThickness="0">
                            <ProgressBar.Template>
                                <ControlTemplate TargetType="ProgressBar">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <Grid>
                                            <Border x:Name="PART_Track"/>
                                            <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="4"
                                                    Background="{TemplateBinding Foreground}"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ProgressBar.Template>
                        </ProgressBar>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- ── Cooling half ─────────────────────────────────────────────── -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel HorizontalAlignment="Center" Margin="0,0,0,14">
                <TextBlock Text="COOLING" Style="{StaticResource SectionTitle}"/>
                <Border Height="3" Width="46" CornerRadius="2" Margin="0,7,0,0"
                        Background="{Binding Theme.AccentColorBrush, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding ObservableFans}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Compact fan row: half the screen means smaller cards than FansScreen -->
                            <Border Background="#0CFFFFFF" BorderBrush="#1AFFFFFF" BorderThickness="1"
                                    CornerRadius="12" Margin="0,0,0,8" Padding="12,9,14,9">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="34" Height="34" CornerRadius="17" Margin="0,0,12,0"
                                            Background="#14FFFFFF" VerticalAlignment="Center">
                                        <Path Data="M12 12c0-3 2.5-5.5 5.5-5.5S23 9 23 12c0 1.4-.5 2.6-1.4 3.6L12 12zm0 0c3 0 5.5 2.5 5.5 5.5S15 23 12 23c-1.4 0-2.6-.5-3.6-1.4L12 12zm0 0c0 3-2.5 5.5-5.5 5.5S1 15 1 12c0-1.4.5-2.6 1.4-3.6L12 12zm0 0c-3 0-5.5-2.5-5.5-5.5S9 1 12 1c1.4 0 2.6.5 3.6 1.4L12 12z"
                                              Fill="{Binding AnimationBrush, FallbackValue='#3b82f6'}"
                                              Width="19" Height="19" Stretch="Uniform" RenderTransformOrigin="0.5,0.5"
                                              HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Path.RenderTransform>
                                                <RotateTransform x:Name="FanRotation" Angle="0"/>
                                            </Path.RenderTransform>
                                            <Path.Triggers>
                                                <EventTrigger RoutedEvent="Loaded">
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetName="FanRotation" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:0.9" RepeatBehavior="Forever"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </Path.Triggers>
                                        </Path>
                                    </Border>

                                    <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center"
                                               Foreground="{Binding DataContext.Theme.ForegroundColor, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue='#FFFFFF'}"
                                               FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Speed, StringFormat={}{0:0}}"
                                                   Foreground="{Binding DataContext.Theme.ForegroundColor, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue='#FFFFFF'}"
                                                   FontSize="19" FontWeight="Bold" VerticalAlignment="Center"/>
                                        <TextBlock Text="RPM"
                                                   Foreground="{Binding DataContext.Theme.ForegroundColor, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue='#FFFFFF'}"
                                                   Opacity="0.45" FontSize="10" FontWeight="Bold"
                                                   VerticalAlignment="Bottom" Margin="3,0,0,3"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
