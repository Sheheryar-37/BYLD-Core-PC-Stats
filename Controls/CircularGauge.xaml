<UserControl x:Class="PcStatsMonitor.Controls.CircularGauge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="250">
    <Grid>
        <!-- Background Track -->
        <Path Stroke="#333333" StrokeThickness="10" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Stretch="None">
            <Path.Data>
                <PathGeometry>
                    <PathFigure StartPoint="125, 235">
                        <ArcSegment Point="25, 125" Size="110,110" SweepDirection="Clockwise" IsLargeArc="True"/>
                    </PathFigure>
                </PathGeometry>
            </Path.Data>
        </Path>

        <!-- Foreground Value Track using a dynamically generated Arc -->
        <Path x:Name="ValuePath" Stroke="{Binding AccentColor, FallbackValue=#FFFFFF}" StrokeThickness="10" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Stretch="None" />
        
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Title, e.g., 25°C -->
            <TextBlock x:Name="ValueText" Foreground="{Binding ForegroundColor, FallbackValue=#FFFFFF}" FontSize="36" FontWeight="{Binding Theme.FontWeight, RelativeSource={RelativeSource AncestorType=Window}, FallbackValue=Normal}" HorizontalAlignment="Center"/>
            <!-- Description, e.g., GPU TEMP -->
            <TextBlock x:Name="TitleText" Foreground="{Binding ForegroundColor, FallbackValue=#FFFFFF}" FontSize="12" Opacity="0.6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
