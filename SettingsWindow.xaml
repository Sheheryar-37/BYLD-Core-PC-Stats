<Window x:Class="PcStatsMonitor.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BYLD Core - Settings" Height="680" Width="620" WindowStartupLocation="CenterScreen" 
        WindowStyle="None" AllowsTransparency="True" Background="#D9161616" ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Icon="Assets/byld-icon.png">
    
    <Window.Resources>
        <SolidColorBrush x:Key="TextBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="BrandBlue" Color="#3b82f6"/>
        <SolidColorBrush x:Key="BrandBlueHover" Color="#2563eb"/>
        
        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#88FFFFFF"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,8,0,8"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="#EEEEEE"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource BrandBlue}"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource BrandBlue}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="{StaticResource BrandBlue}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrandBlue}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource BrandBlueHover}"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource BrandBlueHover}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TabPanel Grid.Row="0" IsItemsHost="True" Background="Transparent" Margin="0,0,0,15"/>
                            <ContentPresenter Grid.Row="1" ContentSource="SelectedContent"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Padding="20,8" Margin="0,0,8,0" CornerRadius="6" Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#22FFFFFF"/>
                                <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="{StaticResource BrandBlue}"/>
                                <Setter TargetName="ContentSite" Property="TextElement.FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#11FFFFFF"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="False"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="White"/>
                            </MultiTrigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="#AAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#0AFFFFFF"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="BorderBrush" Value="#1AFFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="12">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="Assets/byld-icon.png" Width="20" Height="20" Margin="0,0,10,0"/>
                    <TextBlock Text="BYLD CORE SETTINGS" FontWeight="Bold" FontSize="14" Opacity="0.9"/>
                </StackPanel>
                <Button Content="&#x2715;" Width="30" Height="25" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Foreground="#888888" FontWeight="Bold" Click="BtnClose_Click" Style="{x:Null}"/>
            </Grid>

            <TabControl Grid.Row="1">
                <!-- THEME AND COLORS -->
                <TabItem Header="Colors">
                    <StackPanel Margin="5,10">
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="INTERFACE COLORS" Style="{StaticResource SectionHeaderStyle}"/>
                                
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Background Color" VerticalAlignment="Center"/>
                                    <Button x:Name="BtnBgColor" Grid.Column="1" Width="120" Height="28" Click="BtnColorPick_Click" Style="{x:Null}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="#33FFFFFF" BorderThickness="1"/>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                                
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Foreground (Text) Color" VerticalAlignment="Center"/>
                                    <Button x:Name="BtnFgColor" Grid.Column="1" Width="120" Height="28" Click="BtnColorPick_Click" Style="{x:Null}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="#33FFFFFF" BorderThickness="1"/>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>

                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Accent (Glowing Ring) Color" VerticalAlignment="Center"/>
                                    <Button x:Name="BtnAccentColor" Grid.Column="1" Width="120" Height="28" Click="BtnColorPick_Click" Style="{x:Null}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="#33FFFFFF" BorderThickness="1"/>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                                
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Track (Unused Ring) Color" VerticalAlignment="Center"/>
                                    <Button x:Name="BtnTrackColor" Grid.Column="1" Width="120" Height="28" Click="BtnColorPick_Click" Style="{x:Null}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="#33FFFFFF" BorderThickness="1"/>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>

                                <Grid Margin="0,0,0,0">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Alert (Warnings) Color" VerticalAlignment="Center"/>
                                    <Button x:Name="BtnAlertColor" Grid.Column="1" Width="120" Height="28" Click="BtnColorPick_Click" Style="{x:Null}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="#33FFFFFF" BorderThickness="1"/>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </TabItem>

                <!-- LAYOUT -->
                <TabItem Header="Layout">
                    <Grid Margin="5,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.1*"/>
                            <ColumnDefinition Width="0.9*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Margin="0,0,15,0">
                            <Border Style="{StaticResource SectionBorderStyle}">
                                <StackPanel>
                                    <TextBlock Text="DASHBOARD TOGGLES" Style="{StaticResource SectionHeaderStyle}"/>
                                    <CheckBox x:Name="ChkGaugesScreen" Content="Gauges Dashboard" Click="OnScreenSettingChanged"/>
                                    <CheckBox x:Name="ChkStorageScreen" Content="Storage Dashboard" Click="OnScreenSettingChanged"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource SectionBorderStyle}">
                                <StackPanel>
                                    <TextBlock Text="SENSORS" Style="{StaticResource SectionHeaderStyle}"/>
                                    <UniformGrid Columns="2">
                                        <CheckBox x:Name="ChkCpu" Content="CPU" Click="OnSettingChanged"/>
                                        <CheckBox x:Name="ChkGpu" Content="GPU" Click="OnSettingChanged"/>
                                        <CheckBox x:Name="ChkMemory" Content="RAM" Click="OnSettingChanged"/>
                                        <CheckBox x:Name="ChkMotherboard" Content="Board" Click="OnSettingChanged"/>
                                        <CheckBox x:Name="ChkNetwork" Content="Network" Click="OnSettingChanged"/>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource SectionBorderStyle}" Margin="0">
                                <StackPanel>
                                    <TextBlock Text="ROTATION TIMING" Style="{StaticResource SectionHeaderStyle}"/>
                                    <Grid Margin="0,5,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="LblInterval" Text="{Binding Value, ElementName=SldInterval, StringFormat={}{0:0}s}" Foreground="{StaticResource BrandBlue}" FontWeight="Bold" VerticalAlignment="Center"/>
                                        <Slider x:Name="SldInterval" Grid.Column="1" Minimum="3" Maximum="60" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="SldInterval_ValueChanged" VerticalAlignment="Center"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <Border Grid.Column="1" Style="{StaticResource SectionBorderStyle}" Margin="0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="ORDERING &amp; SIZING" Style="{StaticResource SectionHeaderStyle}"/>
                                <Border Grid.Row="1" CornerRadius="6" Background="#11FFFFFF" Margin="0,0,0,15">
                                    <ListBox x:Name="LstOrder" Background="Transparent" Foreground="#FFF" BorderThickness="0" SelectionChanged="LstOrder_SelectionChanged">
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="Padding" Value="0"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <Border x:Name="ItemBorder" Padding="8,6" Background="Transparent" CornerRadius="6" Margin="2">
                                                                <ContentPresenter/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsSelected" Value="True">
                                                                    <Setter TargetName="ItemBorder" Property="Background" Value="#333b82f6"/>
                                                                    <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#3b82f6"/>
                                                                    <Setter TargetName="ItemBorder" Property="BorderThickness" Value="1"/>
                                                                </Trigger>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="ItemBorder" Property="Background" Value="#11FFFFFF"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" Foreground="#EEE" FontSize="11" Margin="0" Cursor="Hand"/>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Border>

                                <!-- Size & Position Config -->
                                <StackPanel x:Name="PnlGaugeConfig" Grid.Row="2" Visibility="Collapsed" Margin="0,0,0,15">
                                    <TextBlock x:Name="LblConfigTitle" Text="SELECTED CONFIG" FontSize="10" Opacity="0.7" Margin="0,0,0,8" FontWeight="Bold" Foreground="{StaticResource BrandBlue}"/>
                                    
                                    <UniformGrid Columns="2" Margin="0,5,0,15">
                                        <!-- Sizing Controls -->
                                        <StackPanel Margin="0,0,10,0" VerticalAlignment="Center">
                                            <TextBlock Text="SIZE" FontSize="9" Opacity="0.6" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button x:Name="BtnGaugeScaleDown" Content="-" Width="36" Height="36" Click="BtnGaugeConfig_Click" Style="{StaticResource ModernButtonStyle}" FontSize="18" FontWeight="Bold" Padding="0"/>
                                                <Button x:Name="BtnGaugeScaleUp" Content="+" Width="36" Height="36" Click="BtnGaugeConfig_Click" Style="{StaticResource ModernButtonStyle}" FontSize="18" FontWeight="Bold" Padding="0" Margin="8,0,0,0"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Positioning (Joystick D-pad) -->
                                        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Up -->
                                            <Button x:Name="BtnGaugeUp" Grid.Row="0" Grid.Column="1" Content="▲" Width="30" Height="30" Margin="2" Click="BtnGaugeConfig_Click" Style="{StaticResource ModernButtonStyle}" FontSize="10" Padding="0"/>
                                            <!-- Left -->
                                            <Button x:Name="BtnGaugeLeft" Grid.Row="1" Grid.Column="0" Content="◀" Width="30" Height="30" Margin="2" Click="BtnGaugeConfig_Click" Style="{StaticResource ModernButtonStyle}" FontSize="10" Padding="0"/>
                                            <!-- Reset/Center -->
                                            <Button x:Name="BtnGaugeReset" Grid.Row="1" Grid.Column="1" Content="•" Width="30" Height="30" Margin="2" Click="BtnGaugeConfig_Click" Style="{StaticResource PrimaryButtonStyle}" FontSize="14" Padding="0" ToolTip="Reset to Default"/>
                                            <!-- Right -->
                                            <Button x:Name="BtnGaugeRight" Grid.Row="1" Grid.Column="2" Content="▶" Width="30" Height="30" Margin="2" Click="BtnGaugeConfig_Click" Style="{StaticResource ModernButtonStyle}" FontSize="10" Padding="0"/>
                                            <!-- Down -->
                                            <Button x:Name="BtnGaugeDown" Grid.Row="2" Grid.Column="1" Content="▼" Width="30" Height="30" Margin="2" Click="BtnGaugeConfig_Click" Style="{StaticResource ModernButtonStyle}" FontSize="10" Padding="0"/>
                                        </Grid>
                                    </UniformGrid>
                                    
                                    <TextBlock Text="Tip: Select a gauge from the list above to adjust." FontSize="8" Opacity="0.4" HorizontalAlignment="Center" FontStyle="Italic"/>
                                </StackPanel>

                                <Grid Grid.Row="3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Content="UP" Margin="0,0,4,0" Click="BtnMoveUp_Click" Style="{StaticResource ModernButtonStyle}" FontSize="10"/>
                                    <Button Grid.Column="1" Content="DOWN" Margin="4,0,0,0" Click="BtnMoveDown_Click" Style="{StaticResource ModernButtonStyle}" FontSize="10"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>

                <!-- IMAGES -->
                <TabItem Header="Images">
                    <StackPanel Margin="5,10">
                        <Border Style="{StaticResource SectionBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="BRANDING AND BACKDROP" Style="{StaticResource SectionHeaderStyle}"/>
                                
                                <TextBlock Text="Logo Image" FontSize="11" Opacity="0.7" Margin="0,0,0,5"/>
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBox x:Name="TxtLogo" IsReadOnly="True" Margin="0,0,5,0" FontSize="10"/>
                                    <Button Grid.Column="1" Content="Browse" Width="70" Click="BtnBrowseLogo_Click" Margin="0,0,5,0" Style="{StaticResource ModernButtonStyle}" FontSize="11"/>
                                    <Button Grid.Column="2" Content="&#x2715;" Width="35" Click="BtnClearLogo_Click" Style="{StaticResource ModernButtonStyle}"/>
                                </Grid>

                                <TextBlock Text="Background Image" FontSize="11" Opacity="0.7" Margin="0,0,0,5"/>
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBox x:Name="TxtBgImage" IsReadOnly="True" Margin="0,0,5,0" FontSize="10"/>
                                    <Button Grid.Column="1" Content="Browse" Width="70" Click="BtnBrowseBg_Click" Margin="0,0,5,0" Style="{StaticResource ModernButtonStyle}" FontSize="11"/>
                                    <Button Grid.Column="2" Content="&#x2715;" Width="35" Click="BtnClearBg_Click" Style="{StaticResource ModernButtonStyle}"/>
                                </Grid>
                                
                                <TextBlock Text="Background Opacity" FontSize="11" Opacity="0.7" Margin="0,0,0,5"/>
                                <Slider x:Name="SldOpacity" Minimum="0.0" Maximum="1.0" TickFrequency="0.1" ValueChanged="SldOpacity_ValueChanged"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </TabItem>
            </TabControl>
            
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Save Settings" Width="130" Height="36" Click="BtnSave_Click" Style="{StaticResource PrimaryButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
